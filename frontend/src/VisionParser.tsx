import * as React from "react";
import { Container, Typography } from "@mui/material";

export default function VisionParser() {
  return (
    <Container>
      <Typography>Vision parser</Typography>
    </Container>
  );
}
